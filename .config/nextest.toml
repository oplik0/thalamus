[profile.default]
retries = 2
test-threads = "num-cpus"
slow-timeout = { period = "60s", terminate-after = 2 }
fail-fast = false

[profile.ci]
retries = 0
fail-fast = true
slow-timeout = { period = "30s" }
